grep of server logs and audit_logs.metadata for this test run confirms no JWT string, cookie value, DATABASE_URL, or JWT_SECRET appears in any log line or audit row -- auditAction's sanitiseBody() strips request bodies to safe fields only; guest tokens are never logged by smokecraftGuestIdentity.js (only guestReference/UUID is ever logged, and only via the audit middleware's actor_id field, never the raw JWT).
