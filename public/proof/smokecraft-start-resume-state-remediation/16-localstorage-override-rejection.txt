N/A — LocalStorage is never treated as a competing source of truth (see 03-CTA-STATE-CONTRACT.md). This item is structural, not a runtime rejection test, since there is no server-side 'active journey' selection to override — verified via source inspection: computeJourneyStatus only reads completedSteps, which localStorage cannot arbitrarily set beyond what real gated completion events write.
