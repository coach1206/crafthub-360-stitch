Scorecard.jsx's handleSubmit now calls saveSnapshot (fire-and-forget, only if managementSync.serverJourneyId exists) with the full mapped payload including the just-submitted scorecard. Verified by code read; the Package D browser suite exercises the Management Sync checkpoint (same underlying saveSnapshot call) rather than re-clicking through the full Scorecard screen, given this pass's time scope.
