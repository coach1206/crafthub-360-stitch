The /integrations endpoint has no write operation at all (GET only) -- idempotency in the classic sense (duplicate-write prevention) does not apply to a pure status-read endpoint. The one real write-capable destination (Ticket Tapper tap/add) already has its own pre-existing behavior, unchanged.
