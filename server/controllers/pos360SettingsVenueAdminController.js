import * as svc from '../services/pos360/pos360SettingsVenueAdminService.js'

const ok500 = (res, fn) => fn().catch(e => res.status(500).json({ ok: false, error: e.message }))
const vid = req => req.headers['x-venue-id'] || req.body?.venueId || req.query?.venueId
const actor = req => req.headers['x-actor-id'] || req.body?.actorId || req.query?.actorId
const tid = req => req.headers['x-tenant-id'] || req.body?.tenantId || req.query?.tenantId

export const createVenueProfile = (req, res) => ok500(res, async () => res.json(await svc.createVenueProfile({ venueId: vid(req), payload: req.body, actorUserId: actor(req), idempotencyKey: req.body?.idempotencyKey })))
export const getVenueProfile = (req, res) => ok500(res, async () => res.json(await svc.getVenueProfile({ venueId: vid(req) })))
export const updateVenueProfile = (req, res) => ok500(res, async () => res.json(await svc.updateVenueProfile({ venueId: vid(req), payload: req.body, actorUserId: actor(req), idempotencyKey: req.body?.idempotencyKey })))
export const createRegionalSettings = (req, res) => ok500(res, async () => res.json(await svc.createRegionalSettings({ venueId: vid(req), payload: req.body, actorUserId: actor(req), idempotencyKey: req.body?.idempotencyKey })))
export const getRegionalSettings = (req, res) => ok500(res, async () => res.json(await svc.getRegionalSettings({ venueId: vid(req) })))
export const updateRegionalSettings = (req, res) => ok500(res, async () => res.json(await svc.updateRegionalSettings({ venueId: vid(req), payload: req.body, actorUserId: actor(req), idempotencyKey: req.body?.idempotencyKey })))
export const createOperatingRule = (req, res) => ok500(res, async () => res.json(await svc.createOperatingRule({ venueId: vid(req), payload: req.body, actorUserId: actor(req), idempotencyKey: req.body?.idempotencyKey })))
export const listOperatingRules = (req, res) => ok500(res, async () => res.json(await svc.listOperatingRules({ venueId: vid(req), filters: req.query })))
export const updateOperatingRule = (req, res) => ok500(res, async () => res.json(await svc.updateOperatingRule({ venueId: vid(req), operatingRuleId: req.params.operatingRuleId, payload: req.body, actorUserId: actor(req), idempotencyKey: req.body?.idempotencyKey })))
export const createFinancialPolicy = (req, res) => ok500(res, async () => res.json(await svc.createFinancialPolicy({ venueId: vid(req), payload: req.body, actorUserId: actor(req), idempotencyKey: req.body?.idempotencyKey })))
export const listFinancialPolicies = (req, res) => ok500(res, async () => res.json(await svc.listFinancialPolicies({ venueId: vid(req), filters: req.query })))
export const updateFinancialPolicy = (req, res) => ok500(res, async () => res.json(await svc.updateFinancialPolicy({ venueId: vid(req), financialPolicyId: req.params.financialPolicyId, payload: req.body, actorUserId: actor(req), idempotencyKey: req.body?.idempotencyKey })))
export const createComplianceSetting = (req, res) => ok500(res, async () => res.json(await svc.createComplianceSetting({ venueId: vid(req), payload: req.body, actorUserId: actor(req), idempotencyKey: req.body?.idempotencyKey })))
export const listComplianceSettings = (req, res) => ok500(res, async () => res.json(await svc.listComplianceSettings({ venueId: vid(req), filters: req.query })))
export const updateComplianceSetting = (req, res) => ok500(res, async () => res.json(await svc.updateComplianceSetting({ venueId: vid(req), complianceSettingId: req.params.complianceSettingId, payload: req.body, actorUserId: actor(req), idempotencyKey: req.body?.idempotencyKey })))
export const createPrivacyNotice = (req, res) => ok500(res, async () => res.json(await svc.createPrivacyNotice({ venueId: vid(req), payload: req.body, actorUserId: actor(req), idempotencyKey: req.body?.idempotencyKey })))
export const listPrivacyNotices = (req, res) => ok500(res, async () => res.json(await svc.listPrivacyNotices({ venueId: vid(req), filters: req.query })))
export const createWhiteLabelProfile = (req, res) => ok500(res, async () => res.json(await svc.createWhiteLabelProfile({ venueId: vid(req), payload: req.body, actorUserId: actor(req), idempotencyKey: req.body?.idempotencyKey })))
export const getWhiteLabelProfile = (req, res) => ok500(res, async () => res.json(await svc.getWhiteLabelProfile({ venueId: vid(req) })))
export const updateWhiteLabelProfile = (req, res) => ok500(res, async () => res.json(await svc.updateWhiteLabelProfile({ venueId: vid(req), whiteLabelProfileId: req.params.whiteLabelProfileId, payload: req.body, actorUserId: actor(req), idempotencyKey: req.body?.idempotencyKey })))
export const createThemeToken = (req, res) => ok500(res, async () => res.json(await svc.createThemeToken({ venueId: vid(req), payload: req.body, actorUserId: actor(req), idempotencyKey: req.body?.idempotencyKey })))
export const listThemeTokens = (req, res) => ok500(res, async () => res.json(await svc.listThemeTokens({ venueId: vid(req), filters: req.query })))
export const updateThemeToken = (req, res) => ok500(res, async () => res.json(await svc.updateThemeToken({ venueId: vid(req), themeTokenId: req.params.themeTokenId, payload: req.body, actorUserId: actor(req), idempotencyKey: req.body?.idempotencyKey })))
export const createModuleRegistryEntry = (req, res) => ok500(res, async () => res.json(await svc.createModuleRegistryEntry({ venueId: vid(req), payload: req.body, actorUserId: actor(req), idempotencyKey: req.body?.idempotencyKey })))
export const listModuleRegistry = (req, res) => ok500(res, async () => res.json(await svc.listModuleRegistry({ venueId: vid(req), filters: req.query })))
export const updateModuleStatus = (req, res) => ok500(res, async () => res.json(await svc.updateModuleStatus({ venueId: vid(req), moduleRegistryId: req.params.moduleRegistryId, status: req.body?.status, actorUserId: actor(req), reason: req.body?.reason, idempotencyKey: req.body?.idempotencyKey })))
export const createModuleGovernanceRule = (req, res) => ok500(res, async () => res.json(await svc.createModuleGovernanceRule({ venueId: vid(req), payload: req.body, actorUserId: actor(req), idempotencyKey: req.body?.idempotencyKey })))
export const listModuleGovernanceRules = (req, res) => ok500(res, async () => res.json(await svc.listModuleGovernanceRules({ venueId: vid(req), filters: req.query })))
export const createFeatureFlagOverride = (req, res) => ok500(res, async () => res.json(await svc.createFeatureFlagOverride({ venueId: vid(req), payload: req.body, actorUserId: actor(req), idempotencyKey: req.body?.idempotencyKey })))
export const listFeatureFlagOverrides = (req, res) => ok500(res, async () => res.json(await svc.listFeatureFlagOverrides({ venueId: vid(req), filters: req.query })))
export const updateFeatureFlagOverrideStatus = (req, res) => ok500(res, async () => res.json(await svc.updateFeatureFlagOverrideStatus({ venueId: vid(req), overrideId: req.params.overrideId, status: req.body?.status, actorUserId: actor(req), reason: req.body?.reason, idempotencyKey: req.body?.idempotencyKey })))
export const createIntegrationStatus = (req, res) => ok500(res, async () => res.json(await svc.createIntegrationStatus({ venueId: vid(req), payload: req.body, actorUserId: actor(req), idempotencyKey: req.body?.idempotencyKey })))
export const listIntegrationStatuses = (req, res) => ok500(res, async () => res.json(await svc.listIntegrationStatuses({ venueId: vid(req), filters: req.query })))
export const updateIntegrationStatus = (req, res) => ok500(res, async () => res.json(await svc.updateIntegrationStatus({ venueId: vid(req), integrationStatusId: req.params.integrationStatusId, status: req.body?.status, actorUserId: actor(req), reason: req.body?.reason, idempotencyKey: req.body?.idempotencyKey })))
export const createProviderReadinessCheck = (req, res) => ok500(res, async () => res.json(await svc.createProviderReadinessCheck({ venueId: vid(req), payload: req.body, actorUserId: actor(req), idempotencyKey: req.body?.idempotencyKey })))
export const listProviderReadinessChecks = (req, res) => ok500(res, async () => res.json(await svc.listProviderReadinessChecks({ venueId: vid(req), filters: req.query })))
export const updateProviderReadinessStatus = (req, res) => ok500(res, async () => res.json(await svc.updateProviderReadinessStatus({ venueId: vid(req), readinessCheckId: req.params.readinessCheckId, status: req.body?.status, actorUserId: actor(req), reason: req.body?.reason, idempotencyKey: req.body?.idempotencyKey })))
export const createAdminConsoleProfile = (req, res) => ok500(res, async () => res.json(await svc.createAdminConsoleProfile({ venueId: vid(req), payload: req.body, actorUserId: actor(req), idempotencyKey: req.body?.idempotencyKey })))
export const listAdminConsoleProfiles = (req, res) => ok500(res, async () => res.json(await svc.listAdminConsoleProfiles({ venueId: vid(req), filters: req.query })))
export const recordAdminSettingsView = (req, res) => ok500(res, async () => res.json(await svc.recordAdminSettingsView({ venueId: vid(req), payload: req.body, actorUserId: actor(req), idempotencyKey: req.body?.idempotencyKey })))
export const createSettingsChangeRequest = (req, res) => ok500(res, async () => res.json(await svc.createSettingsChangeRequest({ venueId: vid(req), payload: req.body, actorUserId: actor(req), idempotencyKey: req.body?.idempotencyKey })))
export const listSettingsChangeRequests = (req, res) => ok500(res, async () => res.json(await svc.listSettingsChangeRequests({ venueId: vid(req), filters: req.query })))
export const applySettingsChangeRequest = (req, res) => ok500(res, async () => res.json(await svc.applySettingsChangeRequest({ venueId: vid(req), changeRequestId: req.params.changeRequestId, actorUserId: actor(req), reason: req.body?.reason, idempotencyKey: req.body?.idempotencyKey })))
export const createSettingsApprovalRequest = (req, res) => ok500(res, async () => res.json(await svc.createSettingsApprovalRequest({ venueId: vid(req), payload: req.body, actorUserId: actor(req), idempotencyKey: req.body?.idempotencyKey })))
export const listSettingsApprovalRequests = (req, res) => ok500(res, async () => res.json(await svc.listSettingsApprovalRequests({ venueId: vid(req), filters: req.query })))
export const decideSettingsApprovalRequest = (req, res) => ok500(res, async () => res.json(await svc.decideSettingsApprovalRequest({ venueId: vid(req), approvalRequestId: req.params.approvalRequestId, managerUserId: req.body?.managerUserId, decision: req.body?.decision, reason: req.body?.reason, idempotencyKey: req.body?.idempotencyKey })))
export const createSettingsVersionHistory = (req, res) => ok500(res, async () => res.json(await svc.createSettingsVersionHistory({ venueId: vid(req), payload: req.body, actorUserId: actor(req), idempotencyKey: req.body?.idempotencyKey })))
export const listSettingsVersionHistory = (req, res) => ok500(res, async () => res.json(await svc.listSettingsVersionHistory({ venueId: vid(req), filters: req.query })))
export const createSettingsRollbackRecord = (req, res) => ok500(res, async () => res.json(await svc.createSettingsRollbackRecord({ venueId: vid(req), versionHistoryId: req.params.versionHistoryId, payload: req.body, actorUserId: actor(req), idempotencyKey: req.body?.idempotencyKey })))
export const decideSettingsRollback = (req, res) => ok500(res, async () => res.json(await svc.decideSettingsRollback({ venueId: vid(req), rollbackRecordId: req.params.rollbackRecordId, managerUserId: req.body?.managerUserId, decision: req.body?.decision, reason: req.body?.reason, idempotencyKey: req.body?.idempotencyKey })))
export const createSettingsExportRequest = (req, res) => ok500(res, async () => res.json(await svc.createSettingsExportRequest({ venueId: vid(req), payload: req.body, actorUserId: actor(req), idempotencyKey: req.body?.idempotencyKey })))
export const listSettingsExportRequests = (req, res) => ok500(res, async () => res.json(await svc.listSettingsExportRequests({ venueId: vid(req), filters: req.query })))
export const markSettingsExportReadyPlaceholder = (req, res) => ok500(res, async () => res.json(await svc.markSettingsExportReadyPlaceholder({ venueId: vid(req), exportRequestId: req.params.exportRequestId, actorUserId: actor(req), idempotencyKey: req.body?.idempotencyKey })))
export const queueOfflineSettingsAction = (req, res) => ok500(res, async () => res.json(await svc.queueOfflineSettingsAction({ venueId: vid(req), payload: req.body, actorUserId: actor(req), idempotencyKey: req.body?.idempotencyKey })))
export const listOfflineSettingsQueue = (req, res) => ok500(res, async () => res.json(await svc.listOfflineSettingsQueue({ venueId: vid(req), filters: req.query })))
export const markOfflineSettingsActionSynced = (req, res) => ok500(res, async () => res.json(await svc.markOfflineSettingsActionSynced({ venueId: vid(req), offlineActionId: req.params.offlineActionId, actorUserId: actor(req), idempotencyKey: req.body?.idempotencyKey })))
