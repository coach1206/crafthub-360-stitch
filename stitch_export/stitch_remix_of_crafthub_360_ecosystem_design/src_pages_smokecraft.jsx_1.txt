import { motion } from 'framer-motion';
import { User, Flame, Compass, CheckCircle2 } from 'lucide-react';

export default function SmokeCraft({ onNavigate }) {
  return (
    <div className="px-6 py-8 h-full flex flex-col">
      <header className="mb-8 flex justify-between items-end">
        <div>
          <h2 className="text-3xl font-bold gold-text uppercase tracking-widest mb-1">SmokeCraft</h2>
          <p className="text-on-surface-variant text-xs tracking-[0.2em] uppercase">Active Session: Founder L0</p>
        </div>
        <div className="bg-primary/20 px-3 py-1 rounded-full border border-primary/30">
          <span className="text-[10px] font-bold text-primary uppercase tracking-tighter">Step 1 of 6</span>
        </div>
      </header>

      <div className="flex-1 space-y-6">
        <div className="glass-panel p-8 rounded-3xl text-center space-y-4">
          <div className="w-20 h-20 bg-primary/10 rounded-full flex items-center justify-center mx-auto border border-primary/20">
            <User className="text-primary" size={32} />
          </div>
          <h3 className="text-2xl font-bold text-white">Mentor Selection</h3>
          <p className="text-on-surface-variant text-sm leading-relaxed">Choose a specialist to guide your sensory humidor journey.</p>
        </div>

        <div className="grid grid-cols-2 gap-4">
          {['Julian V.', 'Elena S.'].map((mentor, i) => (
            <button key={i} className="glass-panel p-6 rounded-2xl flex flex-col items-center gap-3 active:scale-95 transition-transform border-primary/5">
              <div className="w-12 h-12 bg-surface-bright rounded-full" />
              <span className="text-sm font-bold uppercase tracking-widest">{mentor}</span>
              <span className="text-[10px] text-primary uppercase tracking-tighter">Master Tobacconist</span>
            </button>
          ))}
        </div>
      </div>

      <footer className="mt-auto pt-8">
        <button className="w-full h-18 bg-primary text-surface font-black uppercase tracking-[0.3em] rounded-xl flex items-center justify-center gap-4 active:scale-95 transition-transform">
          Begin Induction <ArrowRight size={20} />
        </button>
      </footer>
    </div>
  );
}