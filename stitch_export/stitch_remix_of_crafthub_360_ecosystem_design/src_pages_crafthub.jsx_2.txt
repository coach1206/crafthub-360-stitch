import { motion } from 'framer-motion';
import { Cigarette, Wine, Beer, Beaker, ArrowRight } from 'lucide-react';

export default function CraftHub({ onNavigate }) {
  const modules = [
    { id: 'smokecraft', name: 'SmokeCraft 360', desc: 'Sensory Humidor Journey', icon: <Cigarette size={24} />, color: 'amber' },
    { id: 'winecraft', name: 'WineCraft 360', desc: 'Vintage Vertical Assessment', icon: <Wine size={24} />, color: 'amber' },
    { id: 'beercraft', name: 'BeerCraft 360', desc: 'Palate Intelligence', icon: <Beer size={24} />, color: 'amber' },
    { id: 'pourcraft', name: 'PourCraft 360', desc: 'Mixology Curation', icon: <Beaker size={24} />, color: 'amber' }
  ];

  return (
    <div className="px-6 py-8">
      <header className="mb-12">
        <h2 className="text-3xl font-bold gold-text uppercase tracking-widest mb-2">Module Ecosystem</h2>
        <p className="text-on-surface-variant text-xs tracking-widest uppercase">Select your sensory path</p>
      </header>

      <div className="grid grid-cols-1 gap-4">
        {modules.map((m, i) => (
          <motion.button
            key={m.id}
            initial={{ opacity: 0, x: -20 }}
            animate={{ opacity: 1, x: 0 }}
            transition={{ delay: i * 0.1 }}
            onClick={() => onNavigate(m.id)}
            className="glass-panel p-6 rounded-2xl flex items-center justify-between group active:scale-95 transition-all"
          >
            <div className="flex items-center gap-6">
              <div className="bg-primary/10 p-4 rounded-xl text-primary group-hover:bg-primary group-hover:text-surface transition-colors">
                {m.icon}
              </div>
              <div className="text-left">
                <h3 className="text-xl font-bold text-white mb-1">{m.name}</h3>
                <p className="text-on-surface-variant text-xs uppercase tracking-widest">{m.desc}</p>
              </div>
            </div>
            <ArrowRight className="text-primary/40 group-hover:text-primary transform group-hover:translate-x-2 transition-all" />
          </motion.button>
        ))}
      </div>
    </div>
  );
}