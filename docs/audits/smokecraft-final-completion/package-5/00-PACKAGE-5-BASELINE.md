# Package 5 Baseline

- Branch: `recovery/smokecraft-codex-final` (unchanged) · Commit: `aa0b9cf8` (unchanged) · Uncommitted paths: 188
- **Current applicable routes**: `/smokecraft/wrapper-strength` (`WrapperStrength.jsx`, supporting module, `requires: 'format'`) is currently a dead pass-through — it awards the step and immediately redirects to `/smokecraft/seed-soil`, with no real screen. `/smokecraft/format` (session 5, "Construction Inspection") is the numbered-spine screen closest to construction content but was out of this package's scope to rebuild (verified/working screen, no disclosed defect). No route exists today for wrapper/binder/filler/curing/fermentation/aging as a dedicated live experience.
- **Current leaf/construction catalog content** (already seeded, Package 3): 4 `leaf_priming` (ligero, viso, seco, volado), 3 `wrapper`/`binder`/`filler` role records + `long-filler`/`short-filler`, 3 `processing_method` records (`air-cured`, `pilon-fermentation`, `leaf-aging`), 7 `plant_anatomy`, 9 `vitola`/`ring_gauge`/`length`/`construction_characteristic` records. All real, substantive text — a strong head start for this package.
- **Current curing/fermentation/aging content**: exists as 3 single processing-method records (one each) — no sub-steps, no sorting/grading, no final-resting/box-aging content yet.
- **Missing entirely**: bunching methods (entubado/accordion/book/Lieberman), binder application, molding/pressing, wrapper application, cap construction, foot styles, a connected rolling-process sequence, quality-control/draw-testing checklist, leaf sorting/grading, final resting/box aging.
- **Current quizzes**: 3 total (1 from Package 3 base tied to `ligero`, 2 from Package 4 tied to `criollo`/`volcanic`). None yet for wrapper/binder/filler/construction.
- **Current XP rules**: `seed_soil_quiz_correct` (15), `seed_soil_exploration_complete` (10, unused), plus the original Golden Box rules from migration 077.
- **Current notes/progress structures**: `smokecraft_seed_soil_notes`/`_progress`/`_quiz_attempts` (migration 080) are already generic — keyed by `(guest_reference, component_id)`, not specific to the Seed & Soil screen despite the table name. Reusable as-is for Package 5 without a new migration (see Step 30 disclosure below).
- **Current mentor guidance structures**: `journey.mentor` (array, global), `MentorGuidancePanel`/`EducationalDetailPanel` (Package 2/3, reused unmodified).
- **Current media/hotspot registry**: `smokecraft_hotspots`/`smokecraft_content_media` (migration 079), zero rows — schema-ready only.
- **Current Golden Box blend-component integration**: `golden_box_blend_components.component_type` already accepts `wrapper`, `binder`, `filler`, `leaf_priming`, `curing_method`, `fermentation_method`, `aging_method`, `vitola`, `ring_gauge`, `length`, `construction_characteristic` (migration 077) — the catalog rows this package teaches from are already the exact rows selectable in a live Golden Box entry.
- **Current SC_ASSETS registry**: no per-leaf/construction-step image keys exist yet; `SeedSoil`-style single-composition image pattern is the only precedent.
