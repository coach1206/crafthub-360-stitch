# MVP2 Global Visual Image Registry

This is the source of truth for every uploaded reference image and every
runtime image used by an image-based screen, across all families
(SmokeCraft, POS3, E.A.T., Passport, DayOne360, CraftHub).

## Hard rules (do not violate)

- **Build passing is not visual approval.**
- **Grep passing is not visual approval.**
- **Verification script passing is not visual approval.**
- **Claude saying "looks aligned" is not visual approval.**
- **The rendered proof image (`docs/visual-proof/<screen>-proof.png`,
  generated by `node scripts/visualProof.js`) is the only thing that can
  move a row below from `planned`/`in progress` to `visually approved`.**
- A reference image is a **visual specification** (composition, spacing,
  density, colors, card proportions, background framing, button
  placement, feature layout, premium look, responsive behavior) — it is
  **not** automatically a runtime background. Full UI mockups must be
  rebuilt with React/CSS; only a verified clean-photo region may be used
  as a runtime asset, and never stretched beyond its native aspect ratio.
- Never guess a reference-to-route mapping. If the mapping is unconfirmed,
  the row below says so and the screen is wired with `reference: null` in
  `scripts/visualProof.js` until a human confirms it. (This is the lesson
  from the golden-box route mistake: `GOLDEN BOX JOURNEY11.png` was
  initially miswired to `/smokecraft/golden-box` instead of the correct
  `/smokecraft/golden-box/status`.)

## Status legend

- `planned` — screen/route exists or is scoped, no reference-driven build work done yet.
- `in progress` — UI work has started but no passing proof image yet.
- `visually failed` — a proof image was generated and the rendered side does not match the reference.
- `visually approved` — a proof image was generated and the rendered side matches the reference.
- `blocked — no reference file in repo` — only a text description exists; no binary image to compare against. Visual proof is impossible until a real file is uploaded.
- `blocked — mapping unconfirmed` — one or more candidate reference files exist in `public/` but are not wired into any route in `src/`, and the correct mapping has not been confirmed by a human.

## SmokeCraft

| Reference image | Screen/route | Component file | Reference-only / runtime-safe | Clean photo only? | Contains baked UI? | Approved usage | Banned usage | Runtime image paths | Layout requirements | No-gap requirements | Status |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `PROFILE DISCOVER 11.png` | `/smokecraft` | `src/pages/SmokeCraft.jsx` | Reference-only | No | Yes (full mockup) | Composition/layout spec only | Direct runtime background | n/a | Match reference composition above the fold | No dead margins/black gaps | visually approved (proof: `smokecraft-home-proof.png`) |
| `smokecraft Intake.png` | `/smokecraft/enroll` | `src/pages/smokecraft/Enroll.jsx` | Reference-only | No | Yes (full mockup) | Composition/layout spec only | Direct runtime background | n/a | Match reference composition | No dead margins/black gaps | visually approved (proof: `enroll-proof.png`) |
| `SEED & PAIRING.11.png` | `/smokecraft/seed-soil` | `src/pages/smokecraft/SeedSoil.jsx` | Reference-only | No | Yes (full mockup) | Composition/layout spec only | Direct runtime background | n/a | Seed list, populated Seed Profile, Soil chips, Why This Pairing Works, Pairing Score, Unique Blend Signature, Continue/Back all visible above the fold | No dead margins/black gaps | visually approved (proof: `seed-soil-proof.png`, marker `SEED PAIRING CLEAN BG V3`) |
| `SHAPE SIZE BURN.11.png` | `/smokecraft/format` | `src/pages/smokecraft/Format.jsx` | Reference-only | No | Yes (full mockup) | Composition/layout spec only | Direct runtime background | n/a | Header/progress, title, Format Insight, Cigar Intelligence, Session Comparison, Master Tip, Explore Cigar Formats grid, CTA all visible above the fold | No dead margins/black gaps | visually approved (proof: `format-proof.png`, marker `SHAPE BURN CLEAN BG V2`) |
| none found | `/smokecraft/cigar-gauge-guide` | `src/pages/smokecraft/CigarGaugeGuide.jsx` | n/a | n/a | n/a | n/a | n/a | `/assets/smokecraft/cigars/robusto.jpg` (low-opacity runtime accent only, not a baked mockup) | Sub-step of Session 5 (Shape, Size & Burn Time) — gated by VisitLockGuard stepId `format`, does not advance the 24-session count | n/a | blocked — no reference file in repo (the "cigar gauge guide.png" mockup was shared inline in chat but never committed to the repo as a binary file; reference intentionally left null, not substituted) |
| `GOLDEN BOX JOURNEY11.png` | `/smokecraft/golden-box/status` | `src/pages/smokecraft/GoldenBoxStatus.jsx` | Reference-only | No | Yes (full mockup) | Composition/layout spec only | Direct runtime background | n/a | Match reference composition | No dead margins/black gaps | visually approved (proof: `golden-box-proof.png`) — route corrected from `/smokecraft/golden-box` (commit `b0fd13b`) |
| `lounge demo ranking.11png.png` | `/smokecraft/leaderboard` | `src/pages/smokecraft/Leaderboard.jsx` | Reference-only | No | Yes (full mockup) | Composition/layout spec only | Direct runtime background | n/a | Match reference composition | No dead margins/black gaps | visually approved (proof: `leaderboard-proof.png`) — note: `verifySmokeCraftBannedImages.js` has a pre-existing unrelated FAIL on `scorecard-bg-v2.jpg` (distorted stretch) still open |
| `CUT TOAST, & LIGHT.png` (`public/design-references/mvp2/smokecraft/CUT TOAST, & LIGHT.png`) | `/smokecraft/cut-toast-light` | `src/pages/smokecraft/CutToastLight.jsx` | Reference-only | No | Yes (full mockup) | Composition/layout spec only | Direct runtime background | n/a | TBD — pending audit | TBD — pending audit | mapping confirmed (commit pending), proof not yet generated |
| `venue-management-sync.png` (`public/design-references/mvp2/smokecraft/venue-management-sync.png`) | `/smokecraft/management-sync` | `src/pages/smokecraft/ManagementSync.jsx` | Reference-only | No | Yes (full mockup) | Composition/layout spec only | Direct runtime background | n/a | TBD — pending audit | TBD — pending audit | mapping confirmed, proof not yet generated. **2026-06-27 decision:** `public/EAT SYSTEM UPDATE 11.png` and `public/POS 3.11.png` were considered as alternate references but explicitly rejected — both are E.A.T./POS3 staff/venue operational tool screens (floor-plan editor, order management, humidor inventory), not guest-facing content, and do not match `ManagementSync.jsx` (a guest consent/handoff checklist screen). `venue-management-sync.png` remains the only reference for this route. |
| `final-third-tasting.png` (`public/design-references/mvp2/smokecraft/final-third-tasting.png`) | `/smokecraft/final-third` | `src/pages/smokecraft/FinalThird.jsx` | Reference-only | No | Yes (full mockup) | Composition/layout spec only | Direct runtime background | n/a | TBD — pending audit | TBD — pending audit | mapping confirmed, proof not yet generated. **Duplicate/typo note:** `findal third tasting.png` also exists in `public/assets/smokecraft/` — same pixel dimensions (1672×941) as `final-third-tasting.png` but a different md5/byte size (1,745,803 vs 1,730,074 bytes), i.e. a re-compressed copy of the same mockup under a typo'd filename. The correctly-named `final-third-tasting.png` is used as the canonical reference; the typo'd file is **not** copied into the registry folder. |
| `flavor-dna.png`, originally uploaded as `flavodr dna.png` (`public/design-references/mvp2/smokecraft/flavor-dna.png`) | `/smokecraft/flavor-dna` | `src/pages/smokecraft/FlavorDNA.jsx` | Reference-only | No | Yes (full mockup) | Composition/layout spec only | Direct runtime background | n/a | TBD — pending audit | TBD — pending audit | mapping confirmed, proof not yet generated |
| `passport-connection-1.png` (`public/design-references/mvp2/smokecraft/passport-connection-1.png`) | `/smokecraft/connections` (runtime hero source only — see next row for proof reference) | `src/pages/smokecraft/Connections.jsx` | Reference-only | No | Yes (full mockup) | Used as the source crop for the runtime accent-hero image only (`connections-hero.jpg`); not used as the visual-proof comparison reference (superseded below) | Direct runtime background of the full mockup | `/assets/smokecraft/cropped/connections-hero.jpg` (bounded hero card crop) | TBD — pending audit | TBD — pending audit | superseded as proof reference 2026-06-27; still in active use as runtime hero-crop source |
| `connections-reference.png` (`public/design-references/mvp2/smokecraft/connections-reference.png`, copy of `public/360 PASSPORT NETWORK INTERFACE 2.png`) | `/smokecraft/connections` | `src/pages/smokecraft/Connections.jsx` | Reference-only | No | Yes (full mockup) | Loose/thematic proof-comparison reference only | Direct runtime background; do not treat side-by-side mismatch with rendered page as a defect | n/a | n/a | n/a | mapping confirmed 2026-06-27 per explicit user request. **Content-mismatch note:** this is a full passport-connections *dashboard* mockup (member directory, QR scan, journey-progress rings) — thematically related but NOT a layout match for `Connections.jsx`, which is a single-page consent + action-checklist screen. Used only because no closer match exists in the repo; proof comparisons against this file should not be read as defects. |
| `request-purchase.png` (`public/design-references/mvp2/smokecraft/request-purchase.png`) | `/smokecraft/request-purchase` | `src/pages/smokecraft/RequestPurchase.jsx` | Reference-only | No | Yes (full mockup) | Composition/layout spec only | Direct runtime background | n/a | TBD — pending audit | TBD — pending audit | mapping confirmed, proof not yet generated. Component already correctly uses a *cropped* derivative (`/assets/smokecraft/cropped/request-purchase-bg.jpg`) at runtime — the full mockup is reference-only and must stay that way. |
| `passport-stamp-reference.png` (`public/design-references/mvp2/smokecraft/passport-stamp-reference.png`, copy of `public/360 LUXARY STAMP COLLECT 2.png`) | `/smokecraft/passport-stamp` | `src/pages/smokecraft/PassportStamp.jsx` | Reference-only | No | Yes (full mockup) | Loose/thematic proof-comparison reference only | Direct runtime background; do not treat side-by-side mismatch with rendered page as a defect | n/a | n/a | n/a | mapping confirmed 2026-06-27 per explicit user request. **Content-mismatch note:** this is a "Your Stamp Collection" grid mockup (11/24 stamps across 5 categories) — thematically related but NOT a layout match for `PassportStamp.jsx`, which is a single-page "Your Passport Has Been Certified" completion screen. Used only because no closer match exists in the repo; proof comparisons against this file should not be read as defects. Gated by VisitLockGuard (Visit 8, session 21); proof-only progress is seeded so the real page renders. |

## Passport / 360 Passport Connections

| Reference image | Screen/route | Component file | Reference-only / runtime-safe | Clean photo only? | Contains baked UI? | Approved usage | Banned usage | Runtime image paths | Layout requirements | No-gap requirements | Status |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `connections-reference.png` (`public/design-references/mvp2/passport/connections-reference.png`, copy of `public/360 PASSPORT CONNECTION 2.png`) | `/passport/connections` | `src/pages/passport/PassportConnections.jsx` | Reference-only | No | Yes (full mockup) | Strong structural proof-comparison reference | Direct runtime background | n/a | n/a | n/a | mapping confirmed 2026-06-27. Strong structural match — same hero stats (12 verified / 94% match / 8/10 goal), same tab labels (Best Matches/People You Met/Suggested), same bottom-nav pattern as the live page. Not the same source as `connections-reference.png` under `mvp2/smokecraft/` (that one is a copy of `360 PASSPORT NETWORK INTERFACE 2.png`, used for the unrelated SmokeCraft `/smokecraft/connections` route). |
| `stamps-reference.png` (`public/design-references/mvp2/passport/stamps-reference.png`, copy of `public/DIGETAL STAMP COLLECTION 1.png`) | `/passport/stamps` | `src/pages/passport/PassportStamps.jsx` | Reference-only | No | Yes (full mockup) | Strong structural proof-comparison reference | Direct runtime background | n/a | n/a | n/a | mapping confirmed 2026-06-27. Strong structural match — same 5 stamp categories (Event/Connection/Craft/VIP/Profile Stamps) and 11/24 progress as the live page. **Open question, not yet acted on:** `360 LUXARY STAMP COLLECT 2.png` (a near-duplicate image, currently wired as the SmokeCraft `/smokecraft/passport-stamp` reference) is likely a better structural fit for this route than for the SmokeCraft completion screen it's currently assigned to — left unchanged pending a separate decision. |
| none found | `/passport/profile` | not wired in `src/` | n/a | n/a | n/a | n/a | n/a | n/a | n/a | n/a | blocked — no usable reference file in repo. `DISCOVER YOUR PROFILE.png` / `PROFILE DISCOVER 11.png` were checked and ruled out: they are SmokeCraft homepage mockups, not Passport profile screens |
| none found | `/passport/directory` | not wired in `src/` | n/a | n/a | n/a | n/a | n/a | n/a | n/a | n/a | blocked — no reference file in repo |

## DayOne360

| Reference image | Screen/route | Component file | Reference-only / runtime-safe | Clean photo only? | Contains baked UI? | Approved usage | Banned usage | Runtime image paths | Layout requirements | No-gap requirements | Status |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `concierge-reference.png` (`public/design-references/mvp2/dayone360/concierge-reference.png`, copy of `public/DAYONE360 CONICERGE 1.png`) | `/dayone360` (`/dayone360-travel` renders the identical `DayOneTravel.jsx` component but is not separately tracked in `scripts/visualProof.js`) | `src/pages/DayOneTravel.jsx` | Reference-only | No | Yes (full mockup) | Strong structural proof-comparison reference | Direct runtime background; do not use this asset for any other route | n/a | n/a | n/a | mapping confirmed 2026-06-27. Strong structural match — header/action buttons (Plan Travel/Request Concierge/View Passport), journey tracker (Plan/Pre-Arrival/In-Country/Return), concierge services grid, destination cards, passport stamps section, and bottom nav all match the live page. **Minor non-blocking layout drift (not defects):** (1) reference includes a Concierge Status card not verbatim in the current component; (2) reference includes a 4-tile promo/service row (Travel Booking/Corporate Travel/Relocation Support/Earn More Partner Network) not currently in the live component. |

## CraftHub

| Reference image | Screen/route | Component file | Reference-only / runtime-safe | Clean photo only? | Contains baked UI? | Approved usage | Banned usage | Runtime image paths | Layout requirements | No-gap requirements | Status |
|---|---|---|---|---|---|---|---|---|---|---|---|
| none usable | `/crafthub` | `src/pages/CraftHub.jsx` | n/a | n/a | n/a | n/a | n/a | n/a | n/a | n/a | blocked — no usable full-page reference yet. `CRAFT HUB EXPLAIND.png` was inspected and rejected 2026-06-27: it is a NOVEE OS / EEIE marketing explainer (mood/energy dashboard, EEIE feature list), not the actual `CraftHub.jsx` tile-grid venue hub UI. `crafthub-landing.png` remains assigned to the `BootConsole.jsx` "crafthub" boot-stage asset (see row below) and must not be reused for `/crafthub`. `reference: null` left as-is in `scripts/visualProof.js`. |
| `crafthub-landing.png` (`public/design-references/mvp2/crafthub/crafthub-landing.png`) | boot-stage reference, NOT a routed page — matches the `BootConsole.jsx` "crafthub" boot stage | `src/pages/BootConsole.jsx` (stage id `crafthub`) + `src/components/BootScreen.jsx` | Reference-only | No | Yes (full mockup) | Composition/layout spec only | Direct runtime background; do not create a fake `/crafthub` route for this | n/a (background slot is `public/boot/crafthub-360.png`, explicitly flagged as a pending asset in `BootConsole.jsx` source comments) | TBD — pending audit | TBD — pending audit | **mapped: boot-stage asset, not routed page** — confirmed via `BootConsole.jsx` stage `id: 'crafthub'` (logo, title "CRAFTHUB", connectionItems SmokeCraft/PourCraft/BeerCraft/WineCraft 360 "Connected" exactly matches the mockup's "COMMITTED" rows). No `scripts/visualProof.js` route entry added because the proof harness captures routes, not boot-sequence stages — this is intentional, not a gap. |

## POS3 (handheld + tablet/stationary)

A real reference image (`POS 3.11.png`) was uploaded 2026-06-25 and copied
to `public/design-references/mvp2/pos3/POS 3.11.png`. It is wired to
`/pos3/handheld` (confirmed via `src/App.jsx`: `/pos3/handheld` ->
`src/pages/pos3/POS3Handheld.jsx`) in `scripts/visualProof.js`.

**Important finding from running the proof harness:** `/pos3/handheld`
(and every other `/pos3/*` route) is gated behind a staff-login
`ProtectedRoute` (`requiredPermission="access_pos3_staff"`, `demoBlocked`).
Playwright hits this route unauthenticated, so the rendered capture is an
"ACCESS RESTRICTED" gate screen, not the actual POS3 Handheld UI. The
harness now detects this (`accessGated: true` in the proof JSON) and
prints a warning instead of treating the proof image as a real
comparison. No visual judgment can be made about POS3 Handheld until the
harness can authenticate as a staff user before capturing.

| Reference image | Screen/route | Component file | Reference-only / runtime-safe | Clean photo only? | Contains baked UI? | Approved usage | Banned usage | Runtime image paths | Layout requirements | No-gap requirements | Status |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `POS 3.11.png` (`public/design-references/mvp2/pos3/POS 3.11.png`) | `/pos3/handheld` | `src/pages/pos3/POS3Handheld.jsx` | Reference-only | Unknown — not yet audited | Unknown — not yet audited | Composition/layout spec only, once audited | Direct runtime background | n/a | TBD — pending real (authenticated) capture | TBD — pending real (authenticated) capture | mapping confirmed, proof generated but BEHIND AUTH GATE — not a real screen comparison yet |
| none found | `/pos3/tables` | `src/pages/pos3/POS3Tables.jsx` | n/a | n/a | n/a | n/a | n/a | n/a | TBD | TBD | blocked — no reference file in repo |
| none found | `/pos3/orders` | `src/pages/pos3/POS3Orders.jsx` | n/a | n/a | n/a | n/a | n/a | n/a | TBD | TBD | blocked — no reference file in repo |
| none found | `/pos3/checkout` | `src/pages/pos3/POS3Checkout.jsx` | n/a | n/a | n/a | n/a | n/a | n/a | TBD | TBD | blocked — no reference file in repo |

## E.A.T. System (command screens + seating/table arrangement)

A real reference image (`EAT SYSTEM UPDATE 11.png` — the desktop
management command center) was uploaded 2026-06-25 and copied to
`public/design-references/mvp2/eat-system/EAT SYSTEM UPDATE 11.png`. It is
wired to `/eat/command-hub` (confirmed via `src/App.jsx`: `/eat/command-hub`
-> `src/pages/eat/EATCommandHub.jsx`) as the `eat-command-center` screen
entry in `scripts/visualProof.js`.

**Same auth-gate finding as POS3:** `/eat/*` routes require manager-level
login (`requiredPermission="access_eat_command"`, `demoBlocked`). The
rendered capture for `/eat/command-hub` is an "ACCESS RESTRICTED" gate,
not the real E.A.T. Command Center UI. Flagged via `accessGated: true` in
the proof JSON — not visually approved or failed.

| Reference image | Screen/route | Component file | Reference-only / runtime-safe | Clean photo only? | Contains baked UI? | Approved usage | Banned usage | Runtime image paths | Layout requirements | No-gap requirements | Status |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `EAT SYSTEM UPDATE 11.png` (`public/design-references/mvp2/eat-system/EAT SYSTEM UPDATE 11.png`) | `/eat/command-hub` | `src/pages/eat/EATCommandHub.jsx` | Reference-only | Unknown — not yet audited | Unknown — not yet audited | Composition/layout spec only, once audited | Direct runtime background | n/a | TBD — pending real (authenticated) capture | TBD — pending real (authenticated) capture | mapping confirmed, proof generated but BEHIND AUTH GATE — not a real screen comparison yet |
| none found | `/eat/sections` | `src/pages/eat/EATSections.jsx` | n/a | n/a | n/a | n/a | n/a | n/a | TBD | TBD | blocked — no reference file in repo |
| none found | `/eat/operations` | `src/pages/eat/EATOperations.jsx` | n/a | n/a | n/a | n/a | n/a | n/a | TBD | TBD | blocked — no reference file in repo |

## Other reference files found in repo with no confirmed screen mapping

These exist as real files but are not yet assigned to any route/screen.
Listed here so they are not lost, not so they can be guessed into a
mapping.

| File | Notes |
|---|---|
| `360 story board.png` | Storyboard reference, not wired to any specific screen. |
| `DECK, NOVEE OS.png` | NOVEE OS investor-deck-style reference, not wired to any specific screen. |

## Maintenance

- Every time `node scripts/visualProof.js` (or a `visual:<family>` /
  `visual:all` npm script) is run and a new `<screen>-proof.png` is
  generated and visually inspected, update the relevant row's Status
  column to `visually approved` or `visually failed` — never to
  `visually approved` based on build/grep/script output alone.
- When a new reference image is uploaded, add a row immediately, even if
  every other column is `TBD`, so it is never silently dropped.
- When a Passport/DayOne360/CraftHub mapping is confirmed by a human,
  update both this table and the corresponding entry in
  `scripts/visualProof.js` (change `reference: null` to the real
  filename) in the same commit.
